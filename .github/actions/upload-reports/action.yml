name: 'Generate and Upload Reports'
description: 'Download existing reports, prepare history and upload to GitHub Pages'

inputs:
  browser-name:
    description: 'Browser name for the report'
    required: true
  artifact-name:
    description: 'Name for the artifact upload'
    required: true
  aws-access-key-id:
    description: 'AWS Access Key ID'
    required: true
  aws-secret-access-key:
    description: 'AWS Secret Access Key'
    required: true
  aws-region:
    description: 'AWS Region'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ inputs.aws-access-key-id }}
        aws-secret-access-key: ${{ inputs.aws-secret-access-key }}
        aws-region: ${{ inputs.aws-region }}

    - name: Upload report to S3
      shell: bash
      run: |
        # Create local report directory
        mkdir -p reports/run-${{ github.run_id }}
        cp -r playwright-report/* reports/run-${{ github.run_id }}/

        # Upload to S3
        aws s3 cp reports/run-${{ github.run_id }}/ s3://kaleidos-qa-reports/run-${{ github.run_id }}/ --recursive
        echo "✅ Report uploaded to S3: https://kaleidos-qa-reports.s3.eu-west-1.amazonaws.com/run-${{ github.run_id }}/index.html"

    - name: Checkout gh-pages for index update
      uses: actions/checkout@v4
      with:
        ref: gh-pages
        path: gh-pages
        token: ${{ github.token }}

    - name: Update reports index
      shell: bash
      run: |
        # Calculate Madrid time
        MONTH=$(date -u +%m)
        if [[ "$MONTH" == "04" ]] || [[ "$MONTH" == "05" ]] || [[ "$MONTH" == "06" ]] || \
           [[ "$MONTH" == "07" ]] || [[ "$MONTH" == "08" ]] || [[ "$MONTH" == "09" ]] || [[ "$MONTH" == "10" ]]; then
          DATE_TIME=$(date -u -d "+2 hours" +"%Y-%m-%d %H:%M CEST")
        else
          DATE_TIME=$(date -u -d "+1 hour" +"%Y-%m-%d %H:%M CET")
        fi

        # Get report metadata
        SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
        BRANCH_NAME="${{ github.ref_name }}"
        WORKFLOW_NAME="${{ github.workflow }}"

        # Calculate success percentage
        SUCCESS_PERCENT="N/A"
        SUCCESS_CLASS="success-medium"
        if [ -f "testResults.json" ]; then
          SUCCESS_PERCENT=$(node -e "try { const results = JSON.parse(require('fs').readFileSync('testResults.json', 'utf8')); console.log(Math.round(results.PercentPassed || 0) + '%'); } catch(e) { console.log('N/A'); }")
          if [ "$SUCCESS_PERCENT" != "N/A" ]; then
            PERCENT_NUM=$(echo $SUCCESS_PERCENT | sed 's/%//')
            if [ "$PERCENT_NUM" -ge 90 ]; then
              SUCCESS_CLASS="success-high"
            elif [ "$PERCENT_NUM" -ge 30 ]; then
              SUCCESS_CLASS="success-medium"
            else
              SUCCESS_CLASS="success-low"
            fi
          fi
        fi

        # Set browser icon and workflow label
        BROWSER_ICON="🌐"; BROWSER_LABEL="Chrome"
        if [ "${{ inputs.browser-name }}" = "Firefox" ]; then
          BROWSER_ICON="🦊"; BROWSER_LABEL="Firefox"
        fi

        if [[ "$WORKFLOW_NAME" == "Manual Penpot Tests for PR" ]]; then
          WORKFLOW_LABEL="PR_Manual"
        elif [[ "$WORKFLOW_NAME" =~ firefox|Firefox ]]; then
          WORKFLOW_LABEL="PRE_daily_firefox"
        else
          WORKFLOW_LABEL="PRE_daily"
        fi

        # Create new report row
        cat > /tmp/new_row.html << EOF
          <tr>
            <td class="success-rate $SUCCESS_CLASS" data-label="📊 Success %">$SUCCESS_PERCENT</td>
            <td class="workflow" data-label="🔄 Workflow">$WORKFLOW_LABEL</td>
            <td class="commit" data-label="📝 Commit"><a href="https://github.com/${{ github.repository }}/commit/${{ github.sha }}">$SHORT_SHA</a></td>
            <td class="date" data-label="📅 Date">$DATE_TIME</td>
            <td class="branch" data-label="🌿 Branch">$BRANCH_NAME</td>
            <td class="browser" data-label="🌐 Browser"><span title="$BROWSER_LABEL">$BROWSER_ICON $BROWSER_LABEL</span></td>
            <td data-label="📊 Report"><a href="https://kaleidos-qa-reports.s3.eu-west-1.amazonaws.com/run-${{ github.run_id }}/index.html">🔍 View Report</a></td>
          </tr>
        EOF

        # Extract existing rows and combine with new one
        if [ -f gh-pages/index.html ]; then
          sed -n '/<tbody>/,/<\/tbody>/p' gh-pages/index.html | \
          sed '/<tbody>/d; /<\/tbody>/d; /<!-- ROWS_PLACEHOLDER -->/d; /^[[:space:]]*$/d' > /tmp/existing_rows.html
        else
          touch /tmp/existing_rows.html
        fi

        cat /tmp/new_row.html > /tmp/all_rows.html
        [ -s /tmp/existing_rows.html ] && cat /tmp/existing_rows.html >> /tmp/all_rows.html

        # Generate updated index.html
        cp .github/html-template.html /tmp/template.html
        awk '/<!-- ROWS_PLACEHOLDER -->/ { while ((getline line < "/tmp/all_rows.html") > 0) print line; close("/tmp/all_rows.html"); next } { print }' /tmp/template.html > gh-pages/index.html

        # Cleanup
        rm -f /tmp/new_row.html /tmp/existing_rows.html /tmp/all_rows.html /tmp/template.html

    - name: Upload artifact for backup
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.artifact-name }}
        path: playwright-report/
        retention-days: 7
        if-no-files-found: ignore

    - name: Commit and push index update
      shell: bash
      run: |
        cd gh-pages
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"
        git add .

        if git diff --staged --quiet; then
          echo "No changes to commit"
          exit 0
        fi

        git commit -m "Add report for run ${{ github.run_id }}"

        # Push with retry logic
        for i in {1..3}; do
          echo "Push attempt $i/3..."
          if git push -f origin gh-pages; then
            echo "✅ Successfully pushed to gh-pages"
            break
          else
            if [ $i -lt 3 ]; then
              echo "⚠️ Push failed, retrying..."
              git pull --rebase origin gh-pages
              sleep $((i * 2))
            else
              echo "❌ Failed to push after 3 attempts"
              exit 1
            fi
          fi
        done
